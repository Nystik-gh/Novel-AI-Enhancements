/**
 * An interface defining functions associated with base NAI elements
 *
 * @interface
 */
interface NAIUtils {
    /**
     * Finds and returns the title bar element.
     *
     * @returns {HTMLElement | null} The title bar element if found, otherwise `null`.
     */
    findTitleBar(): HTMLElement | null
}
